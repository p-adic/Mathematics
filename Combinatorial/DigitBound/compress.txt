#ifdef DEBUG
  #include "c:/Users/user/Documents/Programming/Mathematics/Combinatorial/DigitBound/a_Body.hpp"
#else
TE <TY INT>INT MaxDigitBoundedNumber(INT n_max,CRI B,CRI d){AS(n_max >= 0 && B > 1 && d >= 0);INT pw = 1;CO INT div = n_max / B;WH(pw <= div){pw *= B;}INT a = 0;WH(pw > 0){INT c = n_max / pw;if(c <= d){c *= pw;a += c;n_max -= c;pw /= B;}else{a += d *(pw +(pw - 1)/(B - 1));pw = 0;}}RE a;}TE <TY INT>INT CountDigitBoundedNumber(INT n_max,CRI B,int d){n_max = MaxDigitBoundedNumber(MO(n_max),B,d++);INT a = 1,pw = 1;WH(true){a +=(n_max % B)* pw;n_max /= B;if(n_max > 0){pw *= d;}else{break;}}RE a;}
#endif

