// c:/Users/user/Documents/Programming/Mathematics/Arithmetic/Truncated/a.hpp

#pragma once

// O(1)
// 値の上限付き和を計算する。
template <typename INT> inline ll TruncatedSum( const ll& n , const ll& m , const INT& bound );

// O(1)
// 値の上限付き積を計算する。
template <typename INT> inline ll TruncatedProduct( const ll& n , const ll& m , const INT& bound );

