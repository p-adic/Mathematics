#ifdef DEBUG
  #include "c:/Users/user/Documents/Programming/Mathematics/Arithmetic/Mod/Function/Residue/Quotient/a_Body.hpp"
#else
TE <TY INT>pair<INT,ll> CoprimeFactorInverse(CRL base,INT c){AS(base > 0);ll a[2]={0,1 % base};INT b[2]={base,INT((c %= base)< 0?c += base:c)};WH(b[1]!= 0){CO INT q = b[0]/ b[1];(a[0]-= q * a[1]% base)< 0?a[0]+= base:a[0];b[0]-= q * b[1];swap(a[0],a[1]);swap(b[0],b[1]);}RE{MO(a[0]),MO(b[0])};}TE <TY INT> IN INT ModularQuotient(CRL base,CO INT& n,CRL d,CRL coef,CRL gcd){AS(0 <= coef && coef < base &&(d % base * coef - gcd)% base == 0);RE n % gcd == 0?Residue(n / gcd % base * coef,base / gcd):-1;}TE <TY INT> IN INT ModularQuotient(CRL base,CO INT& n,CRL d){auto[coef,gcd]= CoprimeFactorInverse(base,d);RE ModularQuotient(base,n,d,coef,gcd);}
#endif

